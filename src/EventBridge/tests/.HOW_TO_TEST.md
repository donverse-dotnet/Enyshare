1. `tests`フォルダに移動する
    ```ps1
    cd tests
    ```
2. テスト用のコマンドを実行する
    ```ps1
    dotnet test -e MYSQL_CONNECTION_STRING="Server=CHANGE_SERVER_ADDRESS;User=CHANGE_USERNAME;Password=CHANGE_PASSWORD_FOR_MYSQL_USER;Database=EventLogStore;"
    ```
